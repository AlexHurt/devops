apiVersion: v1
kind: Service
metadata:
  name: exposer
  labels:
    app: lab2app
spec:
  selector:
    app: lab2app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer